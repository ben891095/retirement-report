# 退休試算靜態網站

## 1. 專案目標

建立可部署至 GitHub Pages 的單頁退休試算工具。使用者輸入試算年數、現金、投資金額、年報酬率、每月開銷與多筆負債後，網站顯示：

- 退休資金是否足以支撐整段期間
- 現金與總資產耗盡時間
- 可支撐的最高月開銷
- 各筆負債的付清時程
- 資產走勢圖
- 逐年／逐月明細報表

本版不計通膨、退休後其他收入、一次性收支事件及負債利息。月開銷採固定名目金額。

## 2. 技術與檔案結構

採純 HTML、CSS、JavaScript，不使用框架、CDN、第三方依賴或建置流程。

```text
retirement_report/
├── index.html
├── css/style.css
├── js/calc.js       # 純計算，不操作 DOM
├── js/chart.js      # SVG 資產走勢圖
├── js/money.js      # 金額解析與格式化
├── js/ui.js         # 表單、畫面渲染與事件綁定
├── .nojekyll
├── PLAN.md
└── README.md
```

- 使用 `<script type="module">` 載入 `js/ui.js`。
- `index.html` 設定 `lang="zh-Hant"`、viewport 與深色 `color-scheme`。
- favicon 使用 💰 emoji 的 inline SVG data URI。
- 本機需透過靜態伺服器開啟，不支援直接使用 `file://`。

## 3. 名詞與顯示規則

| 名稱 | 定義 |
|---|---|
| 現金 | 每月收支優先進出的金額；可為負，負值代表累計資金缺口 |
| 投資金額 | 產生報酬的本金；可被提領，但不得小於 0 |
| 總資產 | 現金 + 投資金額；可為負 |
| 月報酬率 | 年報酬率 ÷ 12 |
| 投資報酬 | 當月投資產生的收入，存入現金，不併入本金 |
| 剩餘還款總額 | 所有有效負債的 `月付 × 剩餘月份` 合計 |
| 現金耗盡月份 | 現金首次不足以支付當月支出、開始提領投資的月份 |
| 耗盡月份 | 總資產首次小於 0 的月份 |

現金耗盡與總資產耗盡是不同事件，結論中必須分別說明。

金額顯示：

- 表格與圖表：千分位純數字，不加幣別。
- 結論與警示：加 `NT$` 前綴。
- 所有金額以整數元顯示，負值使用警示色。

## 4. 輸入規格

### 4.1 開頁狀態

- 所有欄位留白，只用 `placeholder` 顯示範例。
- 現金、投資金額、年報酬率與每月開銷留白時視為 0。
- 年數必須填寫；填入有效年數後即開始試算。
- 尚未操作的欄位不顯示錯誤。
- 結論、圖表與報表各自顯示空狀態。

### 4.2 欄位

| 區塊 | 欄位 | 規則 | 範例 |
|---|---|---|---|
| 基本 | 還要活多久（年） | 正整數，至少 1，不可留白 | 35 |
| 基本 | 現金 | 非負整數，可留白 | 1,000,000 |
| 收入 | 投資金額 | 非負整數，可留白 | 5,000,000 |
| 收入 | 預期年報酬率（%） | 非負數，最多兩位小數，可留白 | 4 |
| 支出／一般 | 每月開銷 | 非負整數，可留白 | 40,000 |
| 支出／負債 | 每月還款額 | 非負整數，可留白 | 15,000 |
| 支出／負債 | 剩餘月份 | 非負整數，可留白 | 36 |

### 4.3 負債列

- 開頁預設 0 列，可新增、刪除至 0 列。
- 「＋ 新增一筆負債」按鈕，新增一列空白負債。
- 每列右側有「刪除」按鈕，按下即移除該列；**可以刪到 0 列**，0 列即代表無負債。
- 每列具有穩定的內部 `id`，顯示編號則依目前順序重排。
- 每筆負債獨立計算月付與剩餘月份。
- 月付為 0 或剩餘月份為 0 的列不計入還款，也不產生結清事件。
- 剩餘月份可超過試算期間；期末未結清時必須警示。

### 4.4 輸入格式與驗證

- 金額欄使用 `<input type="text" inputmode="numeric" autocomplete="off">`。
- 輸入及貼上時只保留數字，並即時套用千分位。
- 重排千分位後，依「游標左側數字個數」還原游標位置。
- 年數：`type="number" min="1" step="1"`。
- 報酬率：`type="number" min="0" step="0.01"`。
- 剩餘月份：`type="number" min="0" step="1"`。
- 金額解析需移除半形／全形逗號及空白，並區分「0」與「解析失敗」。
- 無效欄位在其下顯示紅色錯誤訊息，訊息容器使用 `aria-live="polite"`。
- 驗證失敗時不重算；保留上一次結果，尚無結果則維持空狀態。

金額格式化統一使用：

```js
Intl.NumberFormat('zh-TW', { maximumFractionDigits: 0 })
```

## 5. 計算模型

### 5.1 核心原則

1. 投資報酬採單利：報酬進入現金，不加入投資本金。
2. 現金不足時提領投資，投資最低為 0。
3. 投資耗盡後，未支付缺口由現金負值承接。
4. 總資產與現金可為負，資金缺口不計利息。
5. 每月生活費及負債還款皆全額扣除。

### 5.2 每月計算順序

總月數 `N = 年數 × 12`，第 1 月為「第 1 年 01 月」。

```text
每月 m（1..N）：
  投資報酬 = max(上月期末投資, 0) × (年報酬率 ÷ 12)
  貸款還款 = 所有剩餘月份 > 0 的有效負債月付合計
  總支出   = 每月開銷 + 貸款還款
  暫計現金 = 上月期末現金 + 投資報酬 - 總支出

  若暫計現金 < 0：
    提領投資 = min(上月期末投資, -暫計現金)
    期末投資 = 上月期末投資 - 提領投資
    期末現金 = 暫計現金 + 提領投資
  否則：
    期末投資 = 上月期末投資
    期末現金 = 暫計現金

  總資產 = 期末現金 + 期末投資

  每筆有效負債的剩餘月份減 1；減至 0 者標記本月結清
  剩餘還款總額 = Σ(月付 × 更新後剩餘月份)
```

補充規則：

- 提領投資後，下個月以降低後的本金計算報酬。
- 投資為 0 後不再產生報酬。
- 負債在結清當月仍支付完整月付，次月起停止扣款。
- 每項當月金額算完後各自使用 `Math.round` 四捨五入至整數元，不提前四捨五入中間值。

### 5.3 最高可支撐月開銷

以整數元二分搜尋，使期末總資產仍大於或等於 0：

- 下界：0。
- 上界：`ceil((現金 + 投資金額 + 全期投資報酬) ÷ N)`。
- 回傳符合條件的最大整數。
- 若月開銷為 0 仍無法支撐，回傳「無解」，表示既有負債已使資產不足。

## 6. 結論與警示

結論卡包含標題、大字結論、註解及逐筆負債時程。

### 6.1 主要狀態

| 狀態 | 必須顯示 |
|---|---|
| 撐得過 | `N 年後仍有 NT$ X`、負債狀態、最高月開銷、固定名目支出與未計通膨提醒 |
| 撐不過 | 資產耗盡年月、期末缺口、距離目標尚差多久、需降低至多少月開銷 |
| 0 開銷仍撐不過 | 缺口來自既有債務，不顯示月開銷上限 |
| 尚未輸入 | `填入「還要活多久」與資產金額，這裡就會顯示結論` |

最高可支撐月開銷恰為 0 時，使用「必須降到 0——完全不花錢才剛好打平」。

### 6.2 現金狀態

- 未提領投資：現金全期足以支付支出，未動用投資本金。
- 由投資支應：說明現金耗盡年月，並補充投資耗盡年月，或提醒本金與報酬會逐月下降。
- 無足夠投資可提領：說明缺口從現金耗盡時開始累積，不得描述為改由投資支應。

### 6.3 負債狀態

- 無負債：顯示「目前無負債」。
- 期內全部結清：顯示最後一筆結清年月。
- 期末仍有負債：在結論卡上方顯示剩餘還款總額警示。
- 剩餘月份大於 0 但月付為 0：警示該列未設定月付、不計入還款。

逐筆時程格式：

- 已結清：`✓ 負債 1（月付 20,000）：第 2 年 12 月付清`
- 未結清：`! 負債 3（月付 15,000）：至期末仍未付清，尚餘 180 期`
- 無月付：`! 負債 N：未設定月付，不計入還款`
- 完全空白列不顯示。

## 7. 報表

採可展開的年列，展開後顯示該年 12 個月。

欄位：

```text
期間｜期初現金｜期初投資金額｜投資報酬｜生活支出｜貸款還款｜期末現金｜期末投資金額｜總資產
```

- 年列：報酬、生活支出與還款取全年合計；期初取首月；期末與總資產取年末月。
- 月列名稱格式為「第 1 年 01 月」。
- 多筆負債的還款欄顯示合計，表頭以 `title` 補充說明。
- 預設收合全部年列，僅在展開時建立月列 DOM。
- 提供「全部展開／全部收合」。
- 月列顯示該月結清的負債；年列同步顯示各筆負債的結清月份。
- 年列可用鍵盤操作，並同步維護 `aria-expanded`。
- 所有新增、刪除與展開按鈕設定 `type="button"`。

## 8. 資產走勢圖

使用原生 SVG，不使用圖表函式庫。

- X 軸：月份，標示年度刻度。
- Y 軸：金額。
- 現金：實線，可低於 0。
- 投資金額：實線，不低於 0。
- 剩餘還款總額：虛線；全期為 0 時不繪製。
- 繪製 `y = 0` 基準線及總資產轉負的紅點與標籤。
- 現金低於 0 的區域標示為「累計資金缺口」。
- 每筆負債結清時繪製垂直虛線；相近標籤需合併或錯開。
- `viewBox` 寬度取容器實際像素寬，透過 `ResizeObserver` 重畫。
- 容器寬度為 0 時使用最小寬度，恢復可見後再重畫。
- 處理全為 0、所有值相同等 Y 軸範圍為 0 的情況。
- 提供螢幕閱讀器摘要：期末現金、投資、總資產、耗盡月份與負債狀態。

## 9. 頁面結構

```text
.page
├── header.page-header
├── form#form
│   ├── 基本
│   ├── 收入
│   └── 支出
│       ├── 一般
│       └── 負債
├── div#warnings
├── section#summary.summary
├── section.panel（資產走勢）
├── section.panel（逐年／逐月報表）
└── footer.page-footer
```

- 頁面最大寬度 1280px，置中顯示。
- 結論卡每次重繪時，正常與空狀態都必須保留「試算結論」標題及 `aria-labelledby` 對應。
- 動態錯誤與警示使用 live region。

## 10. 視覺與響應式規格

### 10.1 設計方向

- 固定暗色、高資訊密度的單欄儀表板。
- 卡片以深灰面板、上緣色條及標題顏色區分。
- 全站使用等寬數字：`font-variant-numeric: tabular-nums`。
- 字族：`"Noto Sans TC", "PingFang TC", "Microsoft JhengHei", system-ui, sans-serif`。
- 基準字級 17px、行高 1.6；任何文字不得小於 14px。
- 所有著色文字對比至少 4.5:1。
- `:focus-visible` 使用 `2px solid var(--accent)`，offset 2px。

### 10.2 色票

```css
:root {
  color-scheme: dark only;
  --bg: #12151a;
  --panel: #1a1f27;
  --panel-2: #212836;
  --border: #2e3644;
  --text: #e9edf3;
  --muted: #b7c0cd;
  --faint: #98a3b3;
  --accent: #5aa9ff;
  --ok: #4ade80;
  --danger: #ff6b6b;
  --warn: #ffc14d;
  --line-cash: #62b6ff;
  --line-invest: #4ade80;
  --line-debt: #c08cff;
  --gap-fill: rgba(255, 107, 107, 0.10);
  --tone-base: #7cc4ff;
  --tone-income: #5ee9a4;
  --tone-expense: #ffc14d;
  --tone-general: #ff9ec4;
  --tone-debt: #c9a6ff;
}
```

現金、投資、負債及生活支出的顏色，必須在圖表、圖例與報表表頭中保持一致。

### 10.3 版面

- `.form-row`：`repeat(auto-fit, minmax(320px, 1fr))`。
- `.grid`：`repeat(auto-fit, minmax(220px, 1fr))`。
- `fieldset` 設定 `min-width: 0`，同列卡片等高但不寫死高度。
- 「支出」為透明外層群組，內含「一般」與「負債」兩張面板。
- 負債列寬版為 `72px 1fr 1fr auto`；`.debt-list` 使用容器查詢，在 430px 以下改為垂直卡片。
- 另保留 `@media (max-width: 640px)` 作為容器查詢退路。
- 報表最小寬度 820px，放在可橫向捲動容器內；頁面本身不得橫向捲動。
- 表頭使用 sticky 定位。
- 卡片圓角 10px，主要面板 12px，輸入框與按鈕 8px。
- 區塊間距約 14–18px，頁面內距為 `24px 20px 64px`。

### 10.4 元件狀態

- `h1` 使用藍→綠→紫漸層文字，並提供不支援時的純色退路。
- 結論卡上緣色條：成功為綠、失敗為紅、空狀態為灰。
- 警示條：左側 4px 黃色邊與淡黃色背景。
- 結清標籤：綠色藥丸樣式。
- 空狀態：置中、14px、使用 `--faint`。
- 系統切換淺色模式時，網站仍維持暗色。

## 11. README 與部署

README 應包含使用方式、模型假設、免責聲明與 GitHub Pages 部署步驟：

1. 建立 GitHub repository 並推送至 `main`。
2. 在 Settings → Pages 選擇 `main` 與 `/ (root)`。
3. 網站路徑為 `https://<user>.github.io/<repo>/`。
4. 保留 `.nojekyll`。

## 12. 驗證標準

### 12.1 計算測例

| # | 輸入 | 期望結果 |
|---|---|---|
| A | 10 年；現金 1,200,000；投資 0；0%；月開銷 10,000；無負債 | 第 120 月期末總資產為 0。改為 11 年後，第 121 月轉負，第 132 月為 -120,000 |
| B | 1 年；現金 500,000；投資 0；0%；月開銷 0；負債月付 10,000、剩 6 月 | 前 6 月扣款，第 6 月結清；第 7 月起還款 0，現金維持 440,000 |
| C | 5 年；現金 0；投資 1,000,000；12%；月開銷 10,000；無負債 | 每月報酬 10,000，投資本金全期維持 1,000,000，現金為 0且不提領本金 |

延伸 C：月開銷改為 20,000 時，每月提領投資補足缺口；第 2 月報酬應為 9,900，而非 10,000。

### 12.2 UI 與邊界測試

- 初始欄位皆空白、無紅字，結論／圖表／報表都有空狀態。
- 有效輸入後即時重算，圖表與報表無 console 錯誤。
- 千分位輸入與貼上正常，游標位置不跳動，非法字元不產生 `NaN`。
- 新增、刪除或重排負債後，穩定 `id` 仍能正確對應結清紀錄。
- 負債為 0 列、月付為 0、剩餘月份為 0、超過試算期間時，結果與警示正確。
- 年列可用滑鼠與鍵盤展開／收合，`aria-expanded` 正確。
- 圖表三條線與報表表頭顏色一致；無負債時不畫負債線。
- 375px 寬時頁面不橫向捲動，負債改為卡片，報表可在自身容器橫向捲動。
- 700–900px 視窗中，負債容器變窄時能依容器寬度切換卡片版面。
- 結論標題在空狀態與重算後都存在。
- 系統偏好為淺色時仍維持暗色，文字對比符合要求。
- 超大金額、全 0、相同值、全期負資產等情況不產生 `NaN` 或版面錯誤。
