# 退休試算靜態網站（GitHub Pages）

## Context

建立一個可放在 GitHub Pages 的靜態退休試算工具：輸入「還要活多久、現金、投資金額、預期年報酬率、每月開銷，以及多筆負債（每月還款額＋剩餘月份）」，拉出逐年／逐月的剩餘金額報表。

已確認不納入：通膨、退休後其他收入、一次性大額事件。本版採**固定名目金額**：月開銷在整段期間維持不變，不隨物價上升。

## 名詞表

後文一律沿用這些名稱，不使用「淨資產」「可投資資產」等其他叫法。

| 名稱 | 定義 |
|---|---|
| 現金 | 可立即動用的金額。每月支出先動用這裡；**可以為負**（代表累計資金缺口） |
| 投資金額 | 投資部位本金。產生報酬，但報酬不併入本金（不複利）；**永遠不會小於 0** |
| 總資產 | 現金 ＋ 投資金額。可以為負 |
| 月報酬率 | 年報酬率 / 12（單利折算） |
| 投資報酬 | 當月由投資金額產生的收入，**存入現金**，不增加投資金額 |
| 月付 | 單一負債列的每月還款額 |
| 剩餘月份 | 單一負債列還要繳幾期；歸 0 即結清 |
| 剩餘還款總額 | Σ(該列月付 × 該列剩餘月份)，全部負債合計 |
| 現金耗盡月份 | 現金首次不足以支應當月支出、開始提領投資的月份 |
| 耗盡月份 | 總資產首次 < 0 的月份 |

「現金耗盡」與「總資產用完」是**兩個不同事件**：現金歸零後仍可提領投資支應，資產尚未見底。兩者都要記錄並在結論卡分別說明。

## 目標成品

```
retirement_report/
├── index.html          # 單頁：輸入表單 + 結論卡 + 圖 + 報表
├── css/style.css
├── js/calc.js          # 純計算，不碰 DOM
├── js/chart.js         # 手繪 SVG 折線圖
├── js/money.js         # 金額格式化 / 解析（表單與報表共用）
├── js/ui.js            # 讀表單、渲染、事件綁定（進入點）
├── .nojekyll           # 避免 GitHub Pages 跑 Jekyll
├── PLAN.md             # 本文件
└── README.md           # 用法 + 部署步驟 + 模型假設與免責聲明
```

無框架、無 CDN、無建置步驟、無第三方依賴、無 `package.json`。網頁以 `<script type="module">` 載入 `js/ui.js`，其餘用 ESM `import`（GitHub Pages 走 https，模組可正常運作；本機需用 `python -m http.server` 開，不能直接 `file://`）。

`index.html` 需設 `<html lang="zh-Hant">`、`<meta name="viewport" content="width=device-width, initial-scale=1">`。

分頁 favicon 用 💰 emoji，以 inline `data:image/svg+xml` URI 內嵌於 `<link rel="icon">`，不額外產生圖檔。

**金額顯示規則**：報表與圖表一律純數字千分位，**不加 `NT$`**；結論卡與警示條的金額**加 `NT$` 前綴**（那裡是給人讀的敘述句，需要單位）。

本文件描述的是**行為與規格**，不指定函式名稱、簽章或模組內部如何切分函式——那些由實作決定。檔案切分、HTML 屬性、CSS 與瀏覽器 API 的指定則屬於規格的一部分，需照做。

## 輸入欄位

分成四個區塊：**基本**、**收入**，以及包在「支出」群組底下的**一般**與**負債**。

**開頁狀態**：所有欄位一律**留白，不帶預設值**，只以 `placeholder` 給範例。開頁時結論卡、圖表、報表各自顯示空狀態文字，且**尚未被碰過的欄位不顯示紅字錯誤**（欄位在 `input` 或 `blur` 後才標記為已碰過）。填入年數後即開始試算。

**基本**

| 欄位 | 說明 | 格式 | 可否為 0 / 留白 | 佔位提示 |
|---|---|---|---|---|
| 還要活多久（年） | 試算期間 | 正整數，≥ 1 | 不可為 0、不可留白 | 例：35 |
| 現金 | 每月支出會先動用 | 非負整數，千分位 | 可為 0；留白視為 0 | 例：1,000,000 |

**收入**

| 欄位 | 說明 | 格式 | 可否為 0 / 留白 | 佔位提示 |
|---|---|---|---|---|
| 投資金額 | 投資部位本金 | 非負整數，千分位 | 可為 0；留白視為 0 | 例：5,000,000 |
| 預期年報酬率（%） | 投資報酬 | 非負數，兩位小數 | 可為 0；留白視為 0 | 例：4 |

- 投資報酬**存到現金裡面**，不滾入投資本金（不複利）。

**支出 → 一般**

| 欄位 | 說明 | 格式 | 可否為 0 / 留白 | 佔位提示 |
|---|---|---|---|---|
| 每月開銷 | 生活支出（大約金額，固定支出＋非固定支出） | 非負整數，千分位 | 可為 0；留白視為 0 | 例：40,000 |

**支出 → 負債**

每一筆是獨立的一列：

| 欄位 | 說明 | 格式 | 可否為 0 / 留白 | 佔位提示 |
|---|---|---|---|---|
| 每月還款額 | 該筆負債每月要繳的錢 | 非負整數，千分位 | 可為 0；留白視為 0 | 例：15,000 |
| 剩餘月份 | 該筆負債還要繳幾期 | 非負整數 | 可為 0；留白視為 0 | 例：36 |

- 可多筆，**預設 0 列**（開頁時列表是空的）。
- 「＋ 新增一筆負債」按鈕在列表下方，新增一列空白負債。
- 每列右側有「刪除」按鈕，按下即移除該列；**可以刪到 0 列**，0 列即代表無負債。
- 月付必須是每筆各自獨立的欄位。
- 每列在建立時配一個內部 `id`，供記錄與計算使用（刪除中間列時，其餘列的結清紀錄要靠這個 `id` 對應）。
- 負債**不計利息**，剩餘月份可以大於試算期間（期末仍未結清，見結論卡與圖表）。

## 計算模型（`js/calc.js`）

### 核心假設（實作時不得自行更改）

1. **投資報酬不複利**——報酬存入現金，投資本金除非被提領否則恆定。
2. **總資產可以為負**——不夾在 0，耗盡後繼續往下累積缺口。
3. **負值由現金承接**——現金不足時先提領投資至 0，之後現金一路往負走；投資永不為負。
4. **資金缺口不計利息**——現金為負不滾利。
5. 因為允許負值，每月支出一律**全額扣除**，不需要「還款與生活費誰先扣」的優先序規則。

### 每月計算順序

試算總月數 `N = 年數 × 12`，第 1 個月即「第 1 年 01 月」。

```
每月 m（1..N）:
  投資報酬 r = max(投資_{m-1}, 0) × (年報酬率 / 12)   // 基礎為上月月末投資金額
  還款       = Σ(該列月付 where 該列剩餘月份 > 0)
  支出       = 還款 + 每月開銷
  現金'      = 現金_{m-1} + r - 支出

  if 現金' < 0:                                      // 先賣投資，投資不為負
      提領   = min(投資_{m-1}, -現金')
      投資_m = 投資_{m-1} - 提領
      現金_m = 現金' + 提領                            // 提領不夠時仍為負
  else:
      投資_m = 投資_{m-1}
      現金_m = 現金'

  總資產_m = 現金_m + 投資_m                           // 可為負

  對每一列負債：若剩餘月份 > 0 則減 1；減完等於 0 者標記「本月結清」
  剩餘還款總額_m = Σ(月付 × 剩餘月份)
```

- 提領投資會讓投資本金變小，**下個月的投資報酬跟著變小**，這個連動必須實作。
- 投資金額為 0 之後不再產生報酬。
- 剩餘月份於**扣款後**才遞減；歸 0 的那個月即結清月，**次月起**不再扣該筆月付。結清當月一律付滿月付，不存在部分支付。
- 月報酬率一律用 `年報酬率 / 12`。

### 四捨五入

每月各金額**各自算完後各自 `Math.round`**，不做「先進位再運算」。
例：投資 1,000,000、年報酬率 4% → 月報酬 3,333.33… → 記為 3,333；現金與投資也各自四捨五入至整數元。這樣可避免浮點尾差造成多出一期付款或錯誤歸零。

### 最高可支撐月開銷

求「在其他輸入不變的前提下，期末總資產仍 ≥ 0 的最大每月開銷」：

- 對每月開銷做**整數元二分搜尋**，下界 0、上界取「現金 + 投資金額 + 全期投資報酬」除以 N 後向上取整。
- 結果為滿足條件的最大整數。
- 若連每月開銷為 0 都撐不過（＝負債還款本身就吃光資產），需明確表示「無解」，讓結論卡能區分「有上限」與「無論如何都撐不過」兩種狀態。

### 邊界情況

- 投資金額為 0 之後不再計投資報酬。
- 負債列的月付為 0 **或** 剩餘月份為 0 → 該列無效，不計入還款，也不會產生結清事件。
- 負債列 0 列 → 視為無負債，一切正常運作。
- 剩餘月份 > N → 期末仍未結清，需在結論卡與圖表反映。

## 金額格式化（`js/money.js`）

本模組對外提供兩種能力，表單與報表共用，避免兩處各寫一份格式化邏輯。函式如何命名與切分由實作決定。

- **格式化**：把數字轉成顯示字串，以 `Intl.NumberFormat('zh-TW', { maximumFractionDigits: 0 })` 產生千分位、不帶小數。
- **解析**：把使用者輸入的字串轉回數字。需去除半形與全形逗號、以及空白；空字串視為 0；無法解析為數字時要能讓呼叫端明確分辨「解析失敗」與「值為 0」這兩種情況。

**輸入框即時千分位**（這是實作上最容易被 silently 打掉的一段，故寫明）：
- 金額欄位用 `<input type="text" inputmode="numeric" autocomplete="off">`——`type="number"` 會直接拒絕含逗號的值，無法做千分位。
- 綁 `input` 事件：解析 → 重新格式化 → 寫回，並**還原游標位置**。做法是格式化前記下「游標左側有幾個數字字元」，寫回後再往右掃到第相同個數字字元的位置。單純設 `selectionStart = 原值` 會在逗號插入時跳位。
- 只允許數字（含貼上時過濾非數字字元）；小數點在金額欄不需要，直接濾掉。
- 百分比欄（預期年報酬率）**不套千分位**，保持 `type="number" min="0" step="0.01"`。
- 年數欄保持 `type="number" min="1" step="1"`。
- 負債的「剩餘月份」欄保持 `type="number" min="0" step="1"`，不套千分位。

**錯誤呈現**：金額欄位解析失敗、年數為 0 或空、報酬率非數字時，於該欄位下方顯示紅色訊息並**阻擋重算**（保留上一次的報表與圖，不清空；若還沒算過任何結果則維持空狀態），訊息容器為 `aria-live="polite"`。**未被碰過的欄位不顯示錯誤**，避免開頁時整排紅字。

## 報表（`js/ui.js`）

年列（可點擊摺疊／展開）→ 展開後為該年 12 個月列。欄位：

`期間 | 期初現金 | 期初投資金額 | 投資報酬 | 生活支出 | 貸款還款 | 期末現金 | 期末投資金額 | 總資產`

- 「總資產」＝期末現金 ＋ 期末投資金額，可為負。
- 多筆負債時，「貸款還款」為**各筆加總**；欄位標題加 `title` 說明。
- 年列數字為該年的**合計**（報酬、支出、還款加總），期初取該年首月、期末取該年末月，總資產取該年末月。
- 月列標籤格式「第 1 年 01 月」。
- 金額套用共用的千分位格式化；負值紅字。
- 預設全部年列摺疊，只有展開的年才建立月列 DOM。
- 「全部展開／全部收合」按鈕。
- 有負債結清的那個月，該月列加標記（如「負債 1 結清 ✓」）；該筆月付自結清**次月**起不再出現。
- **年列也要標示**該年結清的負債與月份（如「負債 1 於 12 月結清 ✓」），不必展開就看得到；同年多筆結清則並列多個標記。
- 所有新增、刪除、展開與收合按鈕明確設定 `type="button"`。年列展開控制必須可用鍵盤操作，並同步更新 `aria-expanded`；動態警告使用適當的 live region，讓輔助技術能讀出變化。

## 結論卡

位於表單與警示條下方，結構為：面板標題「試算結論」→ 大字結論 → 註解清單 → 負債付清時程。金額加 `NT$` 前綴。需涵蓋下列狀態：

| 狀態 | 文案 |
|---|---|
| 撐得過 | 「35 年後仍有 **NT$ X**」＋副行：負債狀態、可支撐的最高月開銷，以及「本試算採固定名目支出、不計通膨，長期結果可能偏樂觀」 |
| 撐不過 | 「資產在 **第 22 年 07 月** 用完」＋期末缺口金額、還差幾年、以及要撐滿需把月開銷降到多少 |
| 連 0 開銷都撐不過（最高可支撐月開銷無解時） | 「即使完全不花錢也撐不過 N 年——缺口來自既有債務還款」，不顯示開銷上限 |
| 無負債 | 負債副行顯示「目前無負債」，不得顯示「第 0 月結清」 |
| 有負債且期內結清 | 「最後一筆負債於第 X 年 Y 月結清」 |
| 負債至期末仍未結清 | 上方獨立警示條：「有負債在試算期間結束時尚未繳完，剩餘還款總額 NT$ X」 |
| 有剩餘月份但月付為 0 | 上方獨立警示條：「第 N 筆負債尚未設定月付，此列不會計入還款」 |
| 尚未輸入（開頁狀態） | 顯示空狀態文字「填入『還要活多久』與資產金額，這裡就會顯示結論」，不顯示任何數字 |

**現金耗盡說明**：結論卡在每種狀態下都要交代現金的去向，三種情況各自不同的文案：

| 情況 | 文案方向 |
|---|---|
| 全期未提領投資 | 「現金全期都夠支付支出，沒有動用到投資本金」 |
| 現金耗盡、改由投資支應 | 「現金在第 X 年 Y 月用完，之後每月改由提領投資支應」＋投資也提領完畢的月份，或提醒投資本金與報酬會逐月下降 |
| 現金耗盡時已無投資可提領（總資產同月或更早轉負） | 「現金在第 X 年 Y 月用完，且沒有足以支應的投資部位，缺口自此開始累積」——不得寫成「改由投資支應」 |

另：最高可支撐月開銷為 0 時，文案改為「必須降到 0——完全不花錢才剛好打平」，不寫「降到 NT$ 0 以內」。

**負債付清時程**：結論卡下方另立一區，**逐筆**列出每一筆負債各自的付清年月，不只顯示最後一筆：

- 期內付清 → 「負債 1（月付 20,000）：第 2 年 12 月 付清」，前置 ✓
- 期末仍未付清 → 「負債 3（月付 15,000）：至期末仍未付清，尚餘 180 期」，前置警示符號
- 月付為 0 → 「負債 N：未設定月付，不計入還款」
- 完全空白的列不列入
- 顯示用的編號依當前列順序（刪除中間列後會重新編號），但對應關係一律靠內部 `id`

## 圖表（`js/chart.js`）

手寫 SVG，無函式庫：
- X 軸＝月份（標年刻度），Y 軸＝金額
- **三條線**，各有專屬顏色並與圖例、報表表頭一致：
  - **現金**（實線）＝每月期末現金，可為負
  - **投資金額**（實線）＝每月期末投資金額，永不為負
  - **負債**（虛線）＝剩餘還款總額 Σ(月付 × 剩餘月份)，逐月遞減，無負債時全為 0 則不繪製
- y=0 基準線；歸零交叉點以紅點與標籤標示。總資產轉負時投資必為 0，故交叉點落在**現金線**上
- 現金低於 0 的區段標示為「**累計資金缺口**」，不稱為負資產或可用透支額度。此缺口不計利息
- 現金與投資金額只代表輸入的部位，不包含房屋、不動產或其他未輸入資產
- 負債結清時點畫垂直虛線（多筆則多條），表示該筆付款將自次月停止；多筆在相近月份結清時合併或錯開標籤，避免文字重疊
- `viewBox` 寬度直接取容器的實際 CSS 像素寬（1 單位 = 1px），換寬度由 `ResizeObserver` 觸發重畫。不用等比縮放，否則刻度文字在 375px 螢幕會被縮到約 3px 而無法閱讀
- SVG 線條與文字顏色一律用 `var()` 主題變數
- 必須處理全為 0、所有值相同等 Y 軸範圍為 0 的情況，並提供可被輔助技術讀取的文字摘要，包括期末現金、期末投資金額、總資產、耗盡月份與負債結清狀態
- 容器寬度為 0（尚未佈局或被隱藏）時退回最小寬度繪製，由 `ResizeObserver` 在可見後重畫

## 頁面結構（DOM 由上而下）

```
.page (max-width 1280px，置中)
├── header.page-header      h1「退休試算」+ p.lede（底部 1px 分隔線）
├── form#form
│   ├── div.form-row        ├─ fieldset[data-tone=base]    「基本」
│   │                       └─ fieldset[data-tone=income]  「收入」
│   └── fieldset.group[data-tone=expense]「支出」
│       └── div.form-row    ├─ fieldset[data-tone=general] 「一般」
│                           └─ fieldset[data-tone=debt]    「負債」
├── div#warnings            警示條（無警示時 hidden）
├── section#summary.summary 結論卡：h2「試算結論」+ 大字 + 註解清單 + 負債付清時程
├── section.panel           h2「資產走勢」+ SVG 圖 + 螢幕閱讀器摘要 + 圖例 + 說明
├── section.panel           h2「逐年／逐月報表」+「全部展開」按鈕 + 可橫捲表格
└── footer.page-footer      免責聲明
```

## 視覺規格（`css/style.css`）

**設計語彙**：固定暗色、資訊密度偏高的單欄儀表板。卡片以深灰面板呈現，靠**上緣色條與標題顏色**分辨區塊，而不是靠大量留白或裝飾。所有數字使用等寬數字（`font-variant-numeric: tabular-nums`），讓上下列對齊。

### 色票（照抄即可重現）

```css
:root {
  color-scheme: dark only;          /* 表單控制項與捲軸也走暗色 */

  --bg: #12151a;                    /* 頁面底 */
  --panel: #1a1f27;                 /* 卡片 / 面板 */
  --panel-2: #212836;               /* 輸入框、年列、負債卡 */
  --border: #2e3644;
  --text: #e9edf3;
  --muted: #b7c0cd;                 /* 次要文字 13–14px */
  --faint: #98a3b3;                 /* 提示、表頭、刻度 11–12px */

  --accent: #5aa9ff;                /* focus 外框 */
  --ok: #4ade80;
  --danger: #ff6b6b;
  --warn: #ffc14d;

  --line-cash: #62b6ff;             /* 圖表：現金 */
  --line-invest: #4ade80;           /* 圖表：投資金額 */
  --line-debt: #c08cff;             /* 圖表：負債 */
  --gap-fill: rgba(255, 107, 107, 0.10);   /* 缺口區塊填色 */

  --tone-base: #7cc4ff;             /* 藍：基本 */
  --tone-income: #5ee9a4;           /* 綠：收入 */
  --tone-expense: #ffc14d;          /* 琥珀：支出群組 */
  --tone-general: #ff9ec4;          /* 玫瑰：支出 一般 */
  --tone-debt: #c9a6ff;             /* 紫：支出 負債 */
}
```

五個 `--tone-*` **彼此不得重複**——「一般」曾與外層「支出」共用琥珀，已改為玫瑰。玫瑰刻意留在暖色系（與父層琥珀同調），但明顯區別於 `--danger` 的紅，避免被誤讀為錯誤狀態。

### 字體與尺寸

字族：`"Noto Sans TC", "PingFang TC", "Microsoft JhengHei", system-ui, sans-serif`；基準 17px / 行高 1.6（原 15px，2026-08 為提升可讀性調大）。**全站字體最小 14px**，無例外（含報表表格、SVG 圖表刻度與標籤）。

| 位置 | 尺寸 | 字重 | 顏色 |
|---|---|---|---|
| `h1` | 30px | 預設 | 漸層文字（藍→綠→紫，96deg） |
| 面板標題 `.panel-title` | 19px | 700 | `--tone-base`，左側 3px 色條 + 10px 內距 |
| `fieldset > legend` | 15px（外層群組維持 14px） | 700 | 各自的 `--tone-*`，字距 0.06em |
| 結論大字 | `clamp(20px, 4.6vw, 30px)` | 700 | 撐得過 `--ok`／撐不過 `--danger` |
| 結論註解 / 付清時程 / 負債列編號 | 14px | 400 | `--muted` |
| 欄位標籤 | 15px | 預設 | `--muted` |
| 提示 `.hint` / 圖例 | 14px | 400 | `--faint` |
| 報表內容與表頭 | 14px | 400（表頭 600） | 表頭依欄位語意上色（見下） |
| 錯誤訊息 | 14px | 400 | `--danger`，`min-height: 1em` 佔位避免跳版 |
| SVG 刻度 / 圖上標籤 / 結清標籤 | 14px | 400 | `--faint` 或 `--danger`（見圖表段落） |
| 頁尾 | 14px | 400 | `--faint` |

SVG 圖表刻度放大後，`js/chart.js` 的內距 `pad.left`／`pad.bottom` 已同步加大（56/74→64/82，34→38），避免 y 軸金額標籤或 x 軸年份標籤被裁切。

### 間距與形狀

- 卡片圓角 10px、結論卡與面板 12px、輸入框與按鈕 8px、結清標籤 999px
- 卡片內距 14px 16px 16px；面板 16px；結論卡 18px 18px 14px
- 區塊之間間隔 14px（表單）／18px（面板）
- 頁面內距 `24px 20px 64px`
- `:focus-visible` 一律 `2px solid var(--accent)`，offset 2px

### 版面與斷點

```
┌─────────────┬─────────────┐
│ 基本        │ 收入        │   ← 同列等高
├─────────────┴─────────────┤
│ 支出（外層群組）           │
│ ┌──────────┬──────────┐  │
│ │ 一般     │ 負債     │  │   ← 同列等高
│ └──────────┴──────────┘  │
└───────────────────────────┘
```

- `.form-row` 用 `grid-template-columns: repeat(auto-fit, minmax(320px, 1fr))`，靠 grid 預設拉伸讓同列等高，**不寫死高度**；空間不足時自動堆疊為單欄。
- 每個 `fieldset` 需加 `min-width: 0`，否則 fieldset 預設的 `min-width: min-content` 會撐破 grid 欄寬。
- 「一般」與「負債」包在外層 `fieldset.group`（legend 為「支出」）內。**外層群組背景透明**，只有內部兩張卡片填 `--panel`，用這個層次表現「屬於同一組」。內層 legend 只寫「一般」「負債」，不重複「支出」二字。
- 欄位格線 `.grid` 用 `repeat(auto-fit, minmax(220px, 1fr))`。
- 報表表格 `min-width: 820px`，包在 `overflow-x: auto` 容器內橫捲；`body` 設 `overflow-x: hidden`，頁面本身永不橫捲。表頭 `position: sticky`。

**負債列**空間足夠時為 `72px 1fr 1fr auto` 的一列多欄，不足時每筆一張卡片（欄位垂直堆疊）。切換依據必須是**容器查詢**（`.debt-list` 設 `container-type: inline-size`，斷點 430px），不是視窗寬度——負債區塊只佔半個版面，用視窗寬度會在中等視窗下容器已很窄卻仍套寬版配置而擠成一團。另保留 `@media (max-width: 640px)` 作為不支援容器查詢時的退路。

### 顏色的語意對應（必須一致）

圖表三條線、圖例色塊、報表表頭三處指的是同一批量，顏色必須相同：

| 量 | 顏色 | 出現位置 |
|---|---|---|
| 現金 | `--line-cash` | 圖表實線、圖例、報表「期初現金／期末現金」表頭 |
| 投資金額 | `--line-invest` | 圖表實線、圖例、報表「期初投資／投資報酬／期末投資」表頭 |
| 負債 | `--line-debt` | 圖表虛線、圖例、報表「貸款還款」表頭 |
| 生活支出 | `--tone-expense` | 報表「生活支出」表頭 |
| 總資產 | `--text` | 報表「總資產」表頭與年列末欄 |

### 元件細節

- **h1 漸層**：`background-clip: text` + 透明文字色，並以 `@supports not (background-clip: text)` 提供純色退路。
- **面板標題**：結論卡、資產走勢、逐年報表三處樣式相同。結論卡的標題為「試算結論」；因為卡片每次重算都會整個清空重繪，**標題必須由重繪流程補回**（正常與空狀態兩條路徑都要），並作為該 section 的 `aria-labelledby` 目標。
- **結論卡上緣色條**顏色隨狀態切換：撐得過綠、撐不過紅、空狀態灰。
- **負債付清時程**：無項目符號，改用 `::before` 放 ✓（`--ok`）或 `!`（`--warn`）。
- **警示條**：左側 4px `--warn` 粗邊，背景 `rgba(255,193,77,0.08)`。
- **結清標籤**：藥丸狀，`rgba(74,222,128,0.14)` 底、`--ok` 字、11px。
- **負值**一律 `--danger` 紅字（報表數字、結論缺口）。
- **空狀態**：`--faint`、14px、置中、上下內距 18px。結論卡、圖表、報表三處都要有。

### 對比要求

所有著色文字維持 4.5:1 以上（含 12px 表頭與圖例）。實測值供對照：`--faint` 對三種底色 5.79–7.17；各 `--tone-*` 對面板 8.18–10.77；報表彩色表頭 6.65–10.23；結論大字 5.96；`--text` 對面板 14.08。

**固定暗色**，不跟隨系統偏好：`color-scheme: dark only` 讓瀏覽器的表單控制項與捲軸也走暗色，並以 `<meta name="color-scheme" content="dark">` 避免 CSS 載入前的白閃。系統偏好切成淺色時頁面仍須維持暗色。

## 部署（寫進 README）

1. `git init` → 建立 GitHub repo → push
2. Settings → Pages → Source 選 `main` 分支 `/ (root)`
3. 網址：`https://<user>.github.io/<repo>/`
4. `.nojekyll` 已包含，避免底線開頭資料夾被略過

## 驗證

### 黃金測例（計算正確性，手算對照）

| # | 輸入 | 期望結果 |
|---|---|---|
| a | 年數 10、現金 1,200,000、投資 0、報酬率 0%、月開銷 10,000、無負債 | 每月現金減 10,000；第 10 年 12 月（第 120 個月）期末總資產恰為 0；若把年數改 11，第 121 個月起轉負，第 132 個月為 -120,000 |
| b | 年數 1、現金 500,000、投資 0、報酬率 0%、月開銷 0、負債一筆（月付 10,000、剩餘 6 個月） | 第 1–6 個月各扣 10,000；第 6 個月標記「結清 ✓」；第 7 個月起還款 0、現金停在 440,000；剩餘還款總額由 60,000 逐月遞減至 0 |
| c | 年數 5、現金 0、投資 1,000,000、報酬率 12%、月開銷 10,000、無負債 | 每月報酬 10,000 恰好打平；**投資本金全程維持 1,000,000**（驗不複利）；現金全程 0，不觸發提領 |

額外檢查：把 c 的月開銷改成 20,000 → 每月缺 10,000，由投資提領，投資逐月下降，且**下月報酬隨之變小**（第 2 個月報酬應為 9,900 而非 10,000）。

### UI 行為

- 起本機靜態服務後開頁：**所有欄位為空、不顯示紅字錯誤**，三處都是空狀態文字；填入年數與金額後即算出結果、年列點擊展開、圖有畫出、console 無錯誤。
- 圖表有三條線（現金／投資金額／負債），顏色與圖例、報表表頭一致；無負債時負債線不繪製。
- 結論卡的「負債付清時程」逐筆列出各自的付清年月；期末未付清者顯示尚餘期數。
- **千分位輸入**：在 `1,000,000` 中間點一下打字，游標不跳位；貼上 `1,234,567` 與 `1234567` 皆可解析；貼上 `abc` 被濾掉不產生 `NaN`。
- 新增／刪除負債列後結果即時重算；刪到 0 列仍正常（視為無負債）；刪除中間某列時，其餘列的結清紀錄仍以穩定 `id` 正確對應。
- 只用鍵盤可新增、刪除及展開／收合年列，`aria-expanded` 狀態正確；動態錯誤與負債警告可由螢幕閱讀器感知。
- 切 mobile 375px 寬：頁面不橫向捲動、負債列改為卡片式、報表表格自身可捲。
- 「基本／收入」與「一般／負債」各自同列等高，且負債列增減後仍維持等高。
- 中等視窗（約 700–900px）下負債區塊約 400px 寬時，負債列已切換為卡片式，欄位不被擠壓。
- 結論卡的「試算結論」標題在空狀態與有結果時都在，重算後不會消失。
- 五個區塊標題顏色兩兩不同，且圖表三條線的顏色與圖例、報表表頭一致。
- 把系統偏好切成淺色，頁面仍維持暗色，且所有小字對比在 4.5:1 以上。
- 邊界輸入：超大金額（1e12）、圖表數值全為 0 或完全相同、總資產全程為負時，不會版面爆掉或出現 `NaN`，圖表文字摘要正確。
- 負債剩餘月份設為大於試算月數時，期末未結清警示條有出現。

## 明確不做（日後可加）

通膨、退休後其他收入（勞保年金／租金）、一次性大額事件、稅、負債名稱欄、報表逐筆負債展開、CSV 匯出、URL 參數分享、localStorage 記憶輸入、自動化測試。

**已知簡化（是刻意的設計，不是 bug）**：
- 投資報酬存入現金，**不滾入本金**（不複利）
- 負債**不計利息**，只用「月付 × 剩餘月份」描述
- 資產可為負，且**資金缺口不滾利息**
- 月報酬率採 `年報酬率 / 12` 的單利折算（略高於幾何折算的年化）

README 必須清楚揭露上述四點，並註明：固定名目支出、不計通膨、固定報酬假設、未計稅負、未計其他退休收入。同時說明「不複利」使結果偏保守、「缺口不滾利」使結果偏樂觀，兩者方向相反，故本工具僅供情境試算、不構成財務建議。

## 變更記錄

- 2026-08-09：UI 微調 —— 全站基準字體 15px→17px（含標題、legend、欄位標籤、hint、錯誤訊息，詳見「字體與尺寸」）；負債列「每月還款額」「剩餘月份」補上 placeholder（例：15,000 / 例：36）；分頁 favicon 改為 💰 emoji（inline SVG data URI）。曾短暫在 `h1` 旁加 💰 icon，後依需求移除，僅保留 favicon。
- 2026-08-09（追加）：發現報表表頭與內容、圖表 SVG 刻度、圖例、頁尾等處仍殘留 11–13px，補齊為**全站最小 14px**；`js/chart.js` 內距同步加大以容納放大後的刻度文字，避免裁切。
